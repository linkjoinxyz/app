.env
.env.*
.idea
.DS_Store
.vscode
env
encrypt.key
__pycache__
.gcloudignore
app.yaml
nginx.conf
google-cloud-sdk
venv
.gstack/
linkjoin-frontend/node_modules/
linkjoin-frontend/dist/
linkjoin-extension/node_modules/
linkjoin-backend/__pycache__/
linkjoin-backend/venv/
linkjoin-backend/jobs.sqlite
.vercel

# Packaged extension builds. These are regenerated from linkjoin-extension/ and
# go stale silently: the committed zip was built before the manifest OAuth client
# id was set, so it did not match the source it claims to package.
dist/
linkjoin-extension.zip
linkjoin-extension-firefox.zip

# Machine-local agent settings (permissions etc). .claude/settings.json and any
# shared skills are intentionally tracked.
.claude/settings.local.json

# Local MCP server config (machine-specific ports)
.mcp.json
